import java.io.*;

public class Bundesliga {
	String Auslesen;
	static String Eingabeh;
	static String Eingabea;
	Auslesen="";

	private static void ladeDatei(String datName) {
    	 File datei = new File(bundesliga.txt);
    	 try {
    	 FileReader leser = new FileReader ("bundesliga.txt");
    	 BufferedReader reader = new BufferedReader(leser);
    	   while (reader.) {
         	  for ( int i = 0;, i < 17, i++) {
              reader.readLine(bundesliga.txt);
    	   }
    	 reader.close();
     }

        	  for ( int i = 0;, i < 17, i++) {
        		  
        	  }
}
}
